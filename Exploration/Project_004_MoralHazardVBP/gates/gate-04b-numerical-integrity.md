# Gate 4b: Numerical Integrity Gate — Verdict

**Date:** 2026-07-03
**Runs after:** Stage 7b (T2 integration test; execution approved at HiL-N2)

**Verdict:** CONDITIONAL PASS [MAJOR]

The simulation itself is faithful, reproducible, transparent, and complete (all six checks pass). The [MAJOR] flag is forced by the **counterexample rule**: a counterexample to P2 (a foundation proposition) was found **within its stated assumptions** (see below), so a clean PASS is not permitted.

**Check summary:**
| Check | Result | Evidence |
|-------|--------|----------|
| N1 Equation–Code Consistency | ✓ | Every function in `model_functions.py` cites its source equation (MP §2/§4/§8, CP P1/P2); sympy re-derived dE_I/dp = λND(s)[1−ε(s)] (P1), branch FOC (1−λ)p = c (MP §8), and the linear tipping price s = a/(2b) — all match the code's numerical behavior (`sympy_checks.csv`, 3/3 PASS). Analytical branch optima reproduced by grid/scipy to <2e-3. |
| N2 Reproducibility | ✓ | Fixed seed 20260703; all parameters in `parameters.py` (classified in `parameter_definitions.md`); one-command rerun documented in the report header; versions in `environment_versions.csv`; every figure generated by `generate_figures.py` from CSVs only (code → CSV → figure traceable); no manual edits. |
| N3 Parameter Transparency | ✓ | All 12 parameters classified (2 THEORETICAL NORMALIZATION, 1 USER SPECIFIED, 9 ILLUSTRATIVE); no parameter described as calibrated; Parameter Change Log empty — no post-results parameter changes. (The regime-classifier code revision and figure-title correction are logged as report §8 deviations; neither changed parameters or data.) |
| N4 Numerical Robustness | ✓ | 2001- vs 4001-point grid: optima and classifications unchanged; scipy bounded optimizer vs grid search agree on every optimum (per-segment optimization with endpoint candidates guards against kink traps); all optimizer runs converged; corner solutions explicitly detected and labeled, never reported as interior. |
| N5 Result Completeness | ✓ | Unfavorable results reported with equal prominence: linear-baseline global optimum at the kink (NOT the expected interior maximum); P6 wedge reversal for ε₀ ≥ 1; P2 counterexample; corner cases enumerated; failed optimizations: none; inconclusive regions: none (stated explicitly). |
| N6 Epistemic Status | ✓ | Every §1–§6 result carries exactly one label (SYMBOLICALLY VERIFIED / NUMERICALLY VERIFIED FOR SPECIFIED PARAMETERS / ROBUST ACROSS SEARCHED GRID / COUNTEREXAMPLE FOUND / NOT A PROOF); no proof language used for numerical results; grid-limited claims state their grids. |

**Epistemic-status audit:**
| Result | Label assigned | Label appropriate? |
|--------|---------------|--------------------|
| P1 elasticity decomposition; branch FOC; linear tipping price | SYMBOLICALLY VERIFIED | Yes — exact sympy identities |
| CRRA expenditure corner; CRRA welfare interior optimum; linear interior local max; linear kink-global optimum; P6 wedge values | NUMERICALLY VERIFIED FOR SPECIFIED PARAMETERS (+ NOT A PROOF) | Yes — point/baseline results |
| P3 sign ∂p̄/∂λ < 0; regime-map classification | ROBUST ACROSS SEARCHED GRID | Yes — grids stated (41-pt λ; 3,600 cells; both price grids) |
| P2 biconditional failure (linear baseline) | COUNTEREXAMPLE FOUND | Yes — assumptions A1–A4 + H2 sufficient condition verified to hold |

**Counterexamples to core propositions:**
- **P2 (Existence and Uniqueness of Originator Exit Threshold)** — the "⇐" direction of Π_O(p) ≥ 0 ⇔ p ≥ p̄ fails for p > p̄_high = 33.28 at the linear-demand baseline, which satisfies A1–A4 and H2's log-concavity sufficient condition. **The original unmodified P2 is BLOCKED FROM STAGE 10** (recorded in `state.json` → `numerical_simulation.blocked_propositions`) until Stage 8 + HiL-6 resolve it. Note: `manuscript.tex` was generated before Stage 7b and contains the unrestricted form; it must not be re-issued without the revision.

**Critical issues (FAILs):** none.

**Warnings to address:** none beyond the counterexample condition.

**Conditions (CONDITIONAL PASS [MAJOR]):**
1. P2 must be revised at Stage 8 / HiL-6 (recommended: restrict the domain to p ∈ [c_g, p_O^max], or restate participation as the interval [p̄_low, p̄_high]) before it — or any figure premised on the one-sided participation form — enters a manuscript.
2. Any manuscript use of the P6 wedge sign must state its inelastic-demand (ε₀ < 1 / essential-drug) scope.

**Recommended action:** Proceed to HiL-N3 (Numerical Results Review); then Stage 8 must triage the handoff block in `numerical_simulation_report.md`.
