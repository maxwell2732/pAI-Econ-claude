# Gate 2: Model Coherence Gate — Verdict

**Verdict:** CONDITIONAL PASS

**Check results:**
| Check | Status | Notes |
|-------|--------|-------|
| 1 — Variable Completeness | ✓ | All symbols defined with explicit domains in Section 6 and the Notation Summary. No undefined symbols. D(·) defined with differentiability and monotonicity; U(·) defined with U' = D⁻¹ (inverse demand relationship). |
| 2 — Equilibrium Concept Compatibility | ✓ | "Constrained Optimization with Rational Expectations (Stackelberg)" is appropriate: complete information game; insurer is the sole strategic agent; patients and originator are price-takers. No BNE or PBE needed. |
| 3 — Budget/Resource Consistency | ✓ | Insurer, patient, originator, and generic firm budget constraints are mutually consistent. Insurer pays λ·p·N·D per period; patients pay (1−λ)p per unit; originator earns (p−c_o)·N·D − F; generics earn zero surplus (competitive). No circular dependencies. |
| 4 — Timing Consistency | ✓ | Insurer sets p (Stage 2) before originator decides (Stage 3) before patients demand (Stage 5). No agent conditions on future events. Insurer's rational expectations of downstream responses are consistent with complete information. |
| 5 — Tension Generation | ✓ | Both tensions are present and generated. Moral hazard channel: D((1−λ)p) > D(c) when (1−λ)p < c; total insurer expenditure λ·p·D((1−λ)p) may rise or fall as p falls (depending on demand elasticity). Participation constraint: π_O(p) < 0 for p < p̄; originator exits. Welfare kink at p̄ is generated by the indicator function in W(p). Cost-minimizing vs. welfare-maximizing gap is generated by comparing the two objective functions. |
| 6 — Existence (Preliminary) | ⚠️ | The originator exit threshold p̄ is defined by (p̄ − c_o)·N·D((1−λ)p̄) = F. For p̄ to exist in (c_g, ∞), the maximum of the left-hand side over p must exceed F: max_p (p−c_o)·N·D((1−λ)p) > F. For linear demand, this holds iff N·(a/(b(1−λ)) − c_o)²·b(1−λ)/4 > F (standard condition). This existence condition must be stated as an explicit assumption in Stage 5. |
| 7 — Payoff Consistency | ⚠️ | Social welfare W(p) has a discrete jump at p̄ (due to −F·1[p≥p̄] + N·V_o·1[p≥p̄]). This is intentional (captures the welfare loss from originator exit) but means W is not differentiable at p̄. The optimization must be solved piecewise (separately on [c_g, p̄) and [p̄, ∞)) with a comparison of limiting values at the kink. This is manageable but must be explicitly handled in Stage 6–7. |

**Critical issues (FAIL checks):** None.

**Issues to address (WARNING checks):**
1. **Check 6 — Existence of p̄**: Add an explicit assumption in Stage 5 (Assumption Audit): "A5 (Existence of participation threshold): The originator's variable profit is large enough to cover fixed costs at the optimal price — i.e., $\max_{p \geq c_g} (p - c_o) \cdot N \cdot D((1-\lambda)p) > F$." For linear demand $D(s) = a - bs$, this is equivalent to $N(a/b(1-\lambda) - c_o)^2 b(1-\lambda)/4 > F$.

2. **Check 7 — Welfare kink at p̄**: Add a proposition in Stage 6–7 that explicitly handles the discontinuity: "Proposition on welfare at the kink: $W(\bar{p}^+) - W(\bar{p}^-) = N \cdot V_o - F$ — the welfare gain from keeping the originator in the market equals the per-patient variety value times N, net of the fixed cost. This gap is positive (negative) iff $N \cdot V_o > F$ ($< F$)."

**Recommended action:** Proceed to HiL-4 (★ HARD STOP) with the following issues to be addressed in Stage 5 (Assumption Audit):
- Add explicit existence assumption for p̄
- Clarify that the welfare optimization is solved piecewise with comparison at the kink
- Document the β = 1 (originator serves full market) simplification as a tractability assumption
